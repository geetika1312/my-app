services:

  backend:
    build: .

    container_name: backend-app

    ports:
      - "5000:5000"

    env_file:
      - .env

    depends_on:
      - redis

    volumes:
      - .:/app
      - /app/node_modules

    command: npm run dev

  redis:
    image: redis:7

    container_name: redis